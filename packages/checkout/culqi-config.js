/**
 * Public Key de Culqi — NO es secreta (está diseñada para ir en el
 * cliente, como la apiKey de Firebase). La Secret Key de Culqi nunca va
 * acá, esa vive en Secret Manager (ver firebase/functions/src/payments).
 */
export const CULQI_PUBLIC_KEY = 'TODO_REEMPLAZAR';
